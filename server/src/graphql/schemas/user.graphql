type Query {
  me: User
}
type Mutation {
  register(email: String!, password: String!): AuthPayload!
  login(email: String!, password: String!): AuthPayload!
}
type User {
  id: ID
  email: String
  image: String
  role: Role
}
type AuthPayload {
  message: String!
}
enum Role {
  ADMIN
  USER
}
